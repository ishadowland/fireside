version: "2"

# golangci-lint config for Fireside. Sprint 0 keeps this lean — only the
# linters that catch real bugs in our wiring code. Sprint 1+ can add
# style / complexity linters once the codebase grows.
#
# v2 schema reference: https://golangci-lint.run/product/migration-guide/
#
# Note: v2 removed `issues.exclude-rules` / `issues.exclusions` entirely.
# To skip generated sqlc files (`internal/store/*.go`) the canonical v2
# path is `//nolint:<linter>` directives in the generated source itself.
# We accept that sqlc output gets linted; if any real issues surface
# they will be addressed per-file rather than via a global exclusion.

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - errcheck       # unchecked errors → silent failures (mandated by docs/design/02-modules.md)
    - govet          # standard go vet
    - ineffassign    # unused assignments
    - staticcheck    # broad static analysis (covers unused, printf format, etc.)
    - unused         # unused vars / funcs / types
  settings:
    errcheck:
      # Allow _ = pattern for now (defer conn.Close() etc.). Tighten in
      # Sprint 1+ once SUB-001 / SUB-003 establish test conventions.
      check-blank: false
      exclude-functions:
        - fmt.Fprintf
        - fmt.Printf

issues:
  # Don't fail CI on the first week of Sprint 0 — let sub-contractors
  # land their PRs and we'll iterate. Sprint 1 sets max-same-issues: 0.
  max-same-issues: 50